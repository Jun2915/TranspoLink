@inject DB db

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link rel="shortcut icon" href="/images/shortcut_icon.png">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="stylesheet" href="/css/pager.css">
    <link rel="stylesheet" href="/css/app.css" asp-append-version="true">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.css" />
    <link rel="stylesheet" href="/css/photo-tools.css" asp-append-version="true" />
    @RenderSection("head", false)
</head>
<body>
    @* Flash Message Container *@
    <div class="info">@TempData["Info"]</div>

    <div class="top-bar">
        <div class="top-bar-item">
            🌐 Language
        </div>

        @* --- USER DROPDOWN LOGIC START --- *@
        @if (User.Identity.IsAuthenticated)
        {
            var identifier = User.Identity.Name;
            var currentUser = db.Users.FirstOrDefault(u => u.Email == identifier || u.Phone == identifier);
            string photoUrl = "/images/default_photo.png";

            if (currentUser is Member m && !string.IsNullOrEmpty(m.PhotoURL))
            {
                if (m.PhotoURL == "default_photo.png") { photoUrl = "/images/default_photo.png"; }
                else { photoUrl = "/images/" + m.PhotoURL; }
            }
            else if (currentUser is Admin a && !string.IsNullOrEmpty(a.PhotoURL))
            {
                if (a.PhotoURL.StartsWith("/images/")) { photoUrl = a.PhotoURL; }
                else { photoUrl = "/images/" + a.PhotoURL; }
            }

            <div class="user-dropdown-container">
                <div class="top-bar-item user-profile-display" id="profileTrigger">
                    <img src="@photoUrl" class="nav-user-photo" alt="Profile" onerror="this.src='/images/default_photo.png'">
                    <span>👋 Welcome, <b>@(currentUser?.Name ?? User.Identity.Name)</b></span>
                    <span style="font-size: 10px; margin-left: 5px;">▼</span>
                </div>

                <div class="profile-dropdown" id="profileDropdown">
                    <div class="dropdown-header">
                        <img src="@photoUrl" class="dropdown-big-photo" onerror="this.src='/images/default_photo.png'">
                        <p class="dropdown-name">@(currentUser?.Name ?? User.Identity.Name)</p>
                        <p class="dropdown-email">@(currentUser?.Email ?? currentUser?.Phone)</p>
                    </div>

                    <div class="dropdown-actions">
                        <a href="/Account/UpdateProfile" class="dropdown-btn btn-manage">Manage Account</a>
                        <a href="/Account/Logout" class="dropdown-btn btn-logout">Log Out</a>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="top-bar-item">
                <a href="/Account/Login">🔐 Login</a> / <a href="/Account/Register">Sign Up</a>
            </div>
        }
    </div>

    <nav>
        <div class="logo">
            <a href="/Home/Index"><img src="/images/shortcut_icon.png" alt="TranspoLink Logo" style="width:50px; height:50px;" /></a>
            <a href="/Home/Index"><span>TranspoLink</span></a>
        </div>
        <ul class="nav-links">
            @if (User.IsInRole("Admin"))
            {
                <li><a href="/Admin">Dashboard</a></li>
                <li><a href="/Admin/Members">Members</a></li>
                <li><a href="/Admin/Routes">Routes</a></li>
                <li><a href="/Admin/Vehicles">Vehicles</a></li>
                <li><a href="/Admin/Trips">Trips</a></li>
                <li><a href="/Admin/Bookings">Bookings</a></li>
            }
            else if (User.IsInRole("Member"))
            {
                <li><a href="/">Home</a></li>
                <li><a href="#features">Features</a></li>
                <li><a href="#trending">News & Offers</a></li>
            }
            else
            {
                <li><a href="/">Home</a></li>
                <li><a href="#features">Features</a></li>
                <li><a href="#trending">News & Offers</a></li>
                <li><a href="#contact">Contact</a></li>
            }
        </ul>
    </nav>

    <main>
        @RenderBody()
    </main>

    <footer>
        Developed by <b>BMIT 2023 Web And Mobile System</b> &middot;
        Copyrighted &copy; @DateTime.Today.Year
    </footer>

    <script src="/js/jquery.min.js"></script>
    <script src="/js/jquery.unobtrusive-ajax.min.js"></script>
    <script src="/js/jquery.validate.min.js"></script>
    <script src="/js/jquery.validate.unobtrusive.min.js"></script>
    <script src="/js/app.js" asp-append-version="true"></script>
    <script src="/js/user.js" asp-append-version="true"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js"></script>
    <script src="/js/photo-tools.js" asp-append-version="true"></script>

    @RenderSection("foot", false)
</body>
</html>