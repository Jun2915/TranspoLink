@model UpdatePasswordVM

@{
    ViewBag.Title += " | Change Password";
}

@section head {
    <link rel="stylesheet" href="/css/forgotpass.css" asp-append-version="true"/>
}

<div class="auth-container">
    <div class="auth-card">
        <h2>Change Password</h2>
        <p>Ensure your account is secure by using a strong password.</p>

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

            <div class="auth-input-group">
                <span class="auth-input-icon">🔒</span>
                <input asp-for="Current" type="password" placeholder="Current Password" autofocus>
            </div>
            <span asp-validation-for="Current" class="field-validation-error"></span>

            <div style="margin-top: 15px;" class="auth-input-group">
                <span class="auth-input-icon">🔑</span>
                <input asp-for="New" type="password" placeholder="New Password">
            </div>
            <span asp-validation-for="New" class="field-validation-error"></span>

            <div class="auth-input-group">
                <span class="auth-input-icon">✅</span>
                <input asp-for="Confirm" type="password" placeholder="Confirm New Password">
            </div>
            <span asp-validation-for="Confirm" class="field-validation-error"></span>

            <button type="submit" class="auth-submit-btn">Update Password</button>

            <div style="margin-top: 20px;">
                <a href="/Account/UpdateProfile" class="auth-link">Cancel & Go Back</a>
            </div>
        </form>
    </div>
</div>