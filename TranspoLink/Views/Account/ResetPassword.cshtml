@model ResetPasswordFinalVM
@{
    ViewBag.Title = "Account | Reset Password";
}

@section head {
    <link rel="stylesheet" href="/css/forgotpass.css" />
}

<div class="auth-container">
    <div class="auth-card">
        <h2>Reset Password</h2>
        <p>Please create a new, strong password for your account.</p>

        <!-- Display Account Info -->
        <div class="account-display">
            Account: @ViewBag.Target
        </div>

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

            <div class="auth-input-group">
                <span class="auth-input-icon">🔑</span>
                <input asp-for="NewPassword" type="password" placeholder="New Password" autofocus>
            </div>
            <span asp-validation-for="NewPassword" class="field-validation-error"></span>

            <div class="auth-input-group">
                <span class="auth-input-icon">🔒</span>
                <input asp-for="ConfirmPassword" type="password" placeholder="Confirm Password">
            </div>
            <span asp-validation-for="ConfirmPassword" class="field-validation-error"></span>

            <button type="submit" class="auth-submit-btn">Update Password</button>
        </form>
    </div>
</div>